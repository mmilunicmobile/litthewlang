c("This program will not actually run but will just give a bunch of errors to demonstrate the error checker."f)
m num test = "test string"f

test = 1f

defo funfunction(m num a, f num b)
    m num returno = a
fino

f bool bloby = funfunction(
    1m, 
    falsa
)